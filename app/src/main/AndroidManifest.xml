<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.QueueManager"
        tools:targetApi="31">
        <activity
            android:name=".SummeryActivity"
            android:exported="false" />
        <!-- <activity -->
        <!-- android:name=".Chooselogin" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".MainActivity" -->
        <!-- android:exported="true" -->
        <!-- android:label="@string/app_name" -->
        <!-- android:theme="@style/Theme.QueueManager.NoActionBar"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <!-- Register your activities -->
        <activity android:name=".BusinessLoginActivity" />
        <activity android:name=".RegisterBusinessActivity" />
        <activity android:name=".CustomerLoginActivity" />
        <activity android:name=".RegisterCustomerActivity" />
        <activity android:name=".BusinessLandingActivity" />
        <activity android:name=".CustomerLandingActivity" />
        <activity android:name=".CustomerQueue" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.QueueManager.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>